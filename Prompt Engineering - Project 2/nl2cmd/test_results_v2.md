# תוצאות בדיקה - גרסה 2

## שינויים בפרומפט

**בעיה שזוהתה בגרסה 1**: המודל מוסיף פרמטרים "חכמים" שלא התבקשו (כמו /Q, /F, caption)

**שיפור שבוצע**: 
```
הוספת כלל: "אל תוסיף פרמטרים שלא התבקשו במפורש בהוראה"
```

## פרומפט מעודכן (v2)

```python
SYSTEM_PROMPT = """אתה מומחה לפקודות Windows CMD.
המשימה שלך: להמיר הוראות בשפה טבעית (עברית) לפקודת CMD אחת.

חוקים:
- החזר רק את הפקודה, ללא הסברים
- פקודה אחת בלבד
- תחביר תקין ל-Windows CMD
- אל תוסיף פרמטרים שלא התבקשו במפורש בהוראה
"""
```

## תוצאות צפויות

אחרי תיקון זה, התרחישים הבעייתיים אמורים להשתפר:
- תרחיש 2: `del downloads\*.tmp` (ללא /Q)
- תרחיש 14: `wmic logicaldisk get size,freespace` (ללא caption)
- תרחיש 16: `taskkill /IM notepad.exe` (ללא /F)

## הרצה

להריץ שוב את הבדיקות:
```bash
python run_tests.py
```

ולהשוות את התוצאות לגרסה 1.
